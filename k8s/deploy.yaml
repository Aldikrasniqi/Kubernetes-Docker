apiVersion: v1
kind: Pod
metadata:
  name: server-user-pod
  labels:
    name: server-user-pod
    app: ctse-app

spec:
  containers:
    - name: server-user-pod
      image: 19980517/ctse-user
      ports:
        - containerPort: 8070
